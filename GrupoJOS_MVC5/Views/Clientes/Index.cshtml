@{
    ViewBag.Title = "Lista de Profissional de Saúde";
    var adm = Request.Cookies["UsuarioADM"].Value;
}

@model List<GrupoJOS_MVC5.Models.Model_Cliente>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h2>@ViewBag.Title</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Index", "Home")">Home</a>
            </li>
            <li>
                <a>Profissional de Saúde</a>
            </li>
            <li class="active">
                <strong>@ViewBag.Title</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content">
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>@ViewBag.Title</h5>
                        <div class="pull-right">
                            <div class="btn-group">
                                <a href="/Clientes/Cadastro">
                                    <button type="button" class="btn btn-xs btn-success">Cadastrar</button>
                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="ibox-content">

                        <table class="table table-striped table-bordered table-hover dataTables-example">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Nome</th>
                                    <th>E-mail</th>
                                    <th>Aniversário</th>
                                    <th>Cidade</th>
                                    <th>Telefones</th>
                                    <th>Contato</th>
                                    <th>Especialidades</th>
                                    <th>Opções</th>
                                </tr>
                            </thead>
                            <tfoot>
                                <tr>
                                    <th>ID</th>
                                    <th>Nome</th>
                                    <th>E-mail</th>
                                    <th>Aniversário</th>
                                    <th>Cidade</th>
                                    <th>Telefones</th>
                                    <th>Contato</th>
                                    <th>Especialidades</th>
                                    <th>Opções</th>
                                </tr>
                            </tfoot>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.idcliente</td>
                                        <td>@item.Nome</td>
                                        <td>@item.Email</td>
                                        <td>@item.Aniversario_m</td>
                                        <td>@item.Cidade</td>
                                        <td>
                                            <span class="label label-default">@item.Fone_Celular</span>
                                            <span class="label label-default">@item.Fone1</span>
                                            <span class="label label-default">@item.Fone2</span>
                                        </td>
                                        <td>@item.Contato</td>
                                        <td>
                                            @if(item.NomeEspecialidade1 != "" || item.NomeEspecialidade1 != null)
                                            {
                                                <span class="label label-info">@item.NomeEspecialidade1</span>
                                            }
                                            @if(item.NomeEspecialidade2 != "" || item.NomeEspecialidade2 != null)
                                            {
                                                <span class="label label-info">@item.NomeEspecialidade2</span>
                                            }
                                            @if(item.NomeEspecialidade3 != "" || item.NomeEspecialidade3 != null)
                                            {
                                                <span class="label label-info">@item.NomeEspecialidade3</span>
                                            }
                                            @if(item.NomeEspecialidade4 != "" || item.NomeEspecialidade4 != null)
                                            {
                                                <span class="label label-info">@item.NomeEspecialidade4</span>
                                            }
                                            @if(item.NomeEspecialidade5 != "" || item.NomeEspecialidade5 != null)
                                            {
                                                <span class="label label-info">@item.NomeEspecialidade5</span>
                                            }
                                        </td>
                                        <td width="15%">
                                            <div class="js-sweetalert">
                                                <a href="/Agenda/Cadastro/@item.idcliente"><button type="button" title="Agendar Visita" class="btn btn-warning dim"><i class="fa fa-clock-o"></i></button></a>
                                                <a href="/Clientes/Editar/@item.idcliente"><button class="btn btn-success dim" type="button"><i class="fa fa-edit"></i></button></a>
                                                @if (adm == "True")
	                                            {<button class="btn btn-danger dim" type="button" data-type="confirm" onclick="AssignButtonClicked(this)" data-assigned-id="@item.idcliente"><i class="fa fa-trash"></i></button>}
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>

                    </div>

                    <script>
                        function AssignButtonClicked(elem) {
                            var id = $(elem).data('assigned-id');
                            swal({
                                title: "Tem certeza?",
                                text: "Esse registro sera apagado e nao podera ser recuperado!",
                                icon: "warning",
                                buttons: { cancel: "Cancelar", ok: "Sim, pode remover!" },
                                dangerMode: true,
                            })
                                .then((willDelete) => {
                                    if (willDelete) {
                                        swal("Removido!", "Registro foi removido com sucesso!", "success")
                                            .then((value) => {
                                                $.post("/Clientes/Index/" + id + "");
                                                window.location = "/Clientes/Index";
                                            });
                                    } else {
                                        swal("Cancelado!", "O registro não foi removido", "error");
                                    }
                                });
                        }
                    </script>

                </div>
            </div>
        </div>

    </div>
</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {

            $('.dataTables-example').DataTable({
                pageLength: 25,
                dom: '<"html5buttons"B>lTfgitp',
                buttons: [
                    { extend: 'copy' },
                    { extend: 'csv' },
                    { extend: 'excel', title: 'ExampleFile' },
                    { extend: 'pdf', title: 'ExampleFile' },

                    {
                        extend: 'print',
                        customize: function (win) {
                            $(win.document.body).addClass('white-bg');
                            $(win.document.body).css('font-size', '10px');

                            $(win.document.body).find('table')
                                .addClass('compact')
                                .css('font-size', 'inherit');
                        }
                    }
                ]

            });



        });

    </script>
}
